package cudamatrix

// BitwiseOr - Perform a bitwise Or operation between two Matrices (return nil on error state)
func (lhs *Matrix) BitwiseOr(rhs *Matrix) (result *Matrix) {

	//lhs.lock.RLock()
	//defer lhs.lock.RUnlock()
	//
	//rhs.lock.RLock()
	//defer rhs.lock.RUnlock()
	//
	//if !lhs.HasSameDimensions(rhs) {
	//	return nil
	//}
	//
	//result = NewMatrix(lhs.data.rows(), lhs.data.cols())
	//
	//gocv.BitwiseOr(lhs.data, rhs.data, &result.data)

	return result

}
