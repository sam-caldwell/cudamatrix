package cudamatrix

// BitwiseXOR - Perform a bitwise XOR operation between two Matrices (return nil on error state)
func (lhs *Matrix) BitwiseXOR(rhs *Matrix) (result *Matrix) {

	//lhs.lock.RLock()
	//defer lhs.lock.RUnlock()
	//
	//rhs.lock.RLock()
	//defer rhs.lock.RUnlock()
	//
	//if !lhs.HasSameDimensions(rhs) {
	//	return nil
	//}
	//
	//result = NewMatrix(lhs.data.rows(), lhs.data.cols())
	//
	//gocv.BitwiseXor(lhs.data, rhs.data, &result.data)

	return result

}
