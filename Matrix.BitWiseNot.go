package cudamatrix

// BitwiseNot - Perform a bitwise Not operation on a Matrix (return nil on error state)
func (lhs *Matrix) BitwiseNot() (result *Matrix) {
	//lhs.lock.RLock()
	//defer lhs.lock.RUnlock()
	//
	//result = NewMatrix(lhs.data.rows(), lhs.data.cols())
	//
	//gocv.BitwiseNot(lhs.data, &result.data)

	return result
}
